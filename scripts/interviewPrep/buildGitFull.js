
const fs = require('fs');
const path = require('path');

const outDir = path.join(__dirname, '../../js/data/interviewPrep');
function q(id, topic, difficulty, question, options, correctAnswer, explanation) {
  return { id, topic, difficulty, question, options, correctAnswer, explanation };
}

const gitQuestions = [

  q('git_arch_1', 'Git Architecture & Object Model', 'Easy',
    'What are the four fundamental object types stored inside the `.git/objects` directory?',
    ['File, Folder, Branch, Tag', 'Blob, Tree, Commit, and Annotated Tag', 'Head, Index, Remote, Config', 'Delta, Patch, Stash, Pack'], 1,
    'Git\'s object database stores 4 core primitives addressed by SHA-1/SHA-256 hashes: Blob (file contents), Tree (directory listing and filenames), Commit (metadata and root tree pointer), and Tag.'),
  q('git_arch_2', 'Git Architecture & Object Model', 'Easy',
    'What does a Git "Blob" object store?',
    ['File metadata, filename, and permissions', 'Pure file content/data without its filename, directory location, or metadata', 'The Git commit message', 'The commit author email'], 1,
    'A Git Blob stores raw file data only. Filenames, directory hierarchies, and file permissions are stored separately in Tree objects.'),
  q('git_arch_3', 'Git Architecture & Object Model', 'Easy',
    'What does the `.git/HEAD` file contain?',
    ['The entire repository source code', 'A reference pointer to the currently checked-out branch or commit (e.g. `ref: refs/heads/main`)', 'The list of deleted files', 'The SSH public key'], 1,
    '`.git/HEAD` points to the branch currently checked out in the working directory (e.g., `ref: refs/heads/main`), or points directly to a commit hash in "detached HEAD" state.'),
  q('git_arch_4', 'Git Architecture & Object Model', 'Easy',
    'What are the Three Trees / Three States of Git?',
    ['Alpha, Beta, Production', 'Working Directory, Staging Area (Index), and Git Repository (.git history)', 'Local, Staging, Cloud', 'Master, Main, Origin'], 1,
    'The 3 states: 1) Working Directory (local files on disk), 2) Staging Area / Index (prepared snapshot in `.git/index`), and 3) Repository / Commit History (committed object database).'),
  q('git_arch_5', 'Git Architecture & Object Model', 'Medium',
    'How does Git uniquely identify objects and verify integrity?',
    ['Sequential integer IDs generated by an auto-increment counter', 'Cryptographic hashing (SHA-1 / SHA-256) of object type, size, and content, creating a content-addressable storage system (Merkle Tree)', 'File modification timestamps', 'Random UUID strings'], 1,
    'Git is a content-addressable filesystem: every object is addressed by the SHA hash of its content. If even 1 byte changes, its hash changes, forming an immutable Merkle Directed Acyclic Graph (DAG).'),
  q('git_arch_6', 'Git Architecture & Object Model', 'Medium',
    'What is a "Detached HEAD" state in Git?',
    ['A fatal error indicating repository corruption', 'A state where HEAD points directly to a specific commit hash rather than a named local branch reference', 'When the remote repository is unreachable', 'When a Git branch is renamed'], 1,
    'In detached HEAD state, HEAD points directly to a specific commit. Any new commits made in this state will not belong to any branch and risk being garbage collected once HEAD is switched away.'),
  q('git_arch_7', 'Git Architecture & Object Model', 'Medium',
    'What is the difference between a Lightweight Tag and an Annotated Tag in Git?',
    ['Lightweight tags can only be created on GitHub', 'A lightweight tag is simply a pointer/bookmark to a commit; an annotated tag is stored as a full Git object containing tagger name, email, date, and GPG signature', 'Annotated tags cannot be deleted', 'Lightweight tags cannot be pushed'], 1,
    'Lightweight tags (`git tag v1.0`) are mere pointers to commits. Annotated tags (`git tag -a v1.0 -m "release"`) create a standalone Git object with author, timestamp, message, and signature verification.'),
  q('git_arch_8', 'Git Architecture & Object Model', 'Medium',
    'What does `git gc` (Garbage Collection) do?',
    ['Deletes uncommitted changes in your working tree', 'Identifies and cleans up unreachable dangling objects, and packs loose objects into efficient compressed `.pack` files with delta compression', 'Removes node_modules folder', 'Deletes all closed branches'], 1,
    '`git gc` packs loose object files into packfiles, generates pack indexes, prunes unreachable/orphan commits older than expiry windows, and optimizes repository disk space.'),
  q('git_arch_9', 'Git Architecture & Object Model', 'Hard',
    'What is a "Packfile" and "Delta Compression" in Git?',
    ['A ZIP file generated for downloading code', 'A mechanism where Git stores full files and subsequent versions as compact byte-level diffs (deltas) against similar files packed into a single binary file (.pack)', 'A tarball for Docker', 'A technique for compressing Git commit messages'], 1,
    'To optimize disk space, Git runs pack generation: it groups similar blobs (revisions of the same file) and stores one full blob plus a series of directed delta diffs in a `.pack` file indexed by `.idx`.'),
  q('git_arch_10', 'Git Architecture & Object Model', 'Hard',
    'How does Git\'s Index (`.git/index`) enable staged commits without writing directly to tree objects until commit time?',
    ['It is an SQLite database storing JSON diffs', 'The Index is a binary file containing a sorted list of file paths, file permissions, timestamps, and corresponding blob SHA-1 hashes representing the proposed next commit state', 'It uploads files directly to GitHub cloud', 'It stores changes in RAM only'], 1,
    'The index is a binary cache mapping file paths to blob SHAs and stat cache info. When you `git add`, blobs are written to `.git/objects` and the index references them. `git commit` writes Tree objects directly from this pre-computed index.'),

  q('git_cmd_1', 'Basic Commands', 'Easy',
    'Which command initializes a new empty Git repository in the current directory?',
    ['git start', 'git create', 'git init', 'git new'], 2,
    '`git init` creates a new Git repository by generating the hidden `.git` directory containing object databases, template files, and reference heads.'),
  q('git_cmd_2', 'Basic Commands', 'Easy',
    'What is the purpose of `git status`?',
    ['Shows the status of server hardware', 'Displays the state of the working directory and staging area, showing modified, untracked, and staged files', 'Checks internet ping to GitHub', 'Lists all team members and their emails'], 1,
    '`git status` inspects working tree changes against the staging area index and current HEAD commit, identifying tracked, staged, modified, and untracked files.'),
  q('git_cmd_3', 'Basic Commands', 'Easy',
    'What does `git diff` without any arguments show?',
    ['Differences between the staging area and the last commit', 'Differences between your working directory (unstaged changes) and the staging area (index)', 'Differences between two remote branches', 'A list of commit messages'], 1,
    '`git diff` without flags compares your working directory against the staging area (showing what modifications you made that have NOT yet been staged with `git add`).'),
  q('git_cmd_4', 'Basic Commands', 'Easy',
    'Which flag allows staging all tracked and untracked modified files simultaneously?',
    ['git add .', 'git commit -m', 'git push -u', 'git stage -all'], 0,
    '`git add .` (or `git add -A`) stages all new, modified, and deleted files in the current directory tree for the next commit.'),
  q('git_cmd_5', 'Basic Commands', 'Medium',
    'What is the difference between `git diff` and `git diff --staged` (or `--cached`)?',
    ['`git diff` is faster', '`git diff` shows unstaged changes in working tree; `git diff --staged` shows differences between the staged changes in index and the last commit (what will be committed)', '`git diff --staged` deletes changes', 'They are exact synonyms'], 1,
    '`git diff --staged` (or `--cached`) reveals exactly what changes are currently sitting in the staging area ready to go into the next commit compared to HEAD.'),
  q('git_cmd_6', 'Basic Commands', 'Medium',
    'What does `git log --oneline --graph --decorate --all` do?',
    ['Deletes merged branches', 'Renders an ASCII visualization of all branches, commits, and tags in a compact one-line format showing branch divergence and merges', 'Searches for passwords in commit history', 'Clones a remote repository'], 1,
    'This command creates a clean terminal graph of the entire repository history, showing commit hashes, branch pointers, tags, and merge points across all local and remote branches.'),
  q('git_cmd_7', 'Basic Commands', 'Medium',
    'What does the command `git commit --amend` allow you to do?',
    ['Delete the last 5 commits', 'Combine staged changes into the most recent commit and/or modify the last commit message without creating a brand new commit node', 'Push changes directly to production', 'Revert the working tree to initial state'], 1,
    '`git commit --amend` replaces the tip of the current branch by creating a new commit with staged fixes and an updated message, incorporating changes into the previous commit.'),
  q('git_cmd_8', 'Basic Commands', 'Medium',
    'What does the `.gitignore` file do and why does it ignore files that are already tracked?',
    ['It deletes ignored files permanently from disk', 'It tells Git to ignore untracked files matching specified patterns; it does NOT ignore files that are already tracked in Git index unless they are untracked via `git rm --cached`', 'It encrypts sensitive files', 'It hides files in Windows Explorer'], 1,
    '`.gitignore` specifies intentionally untracked patterns (build artifacts, `.env`, `node_modules`). It only applies to untracked files; already-tracked files must be removed from the index via `git rm --cached <file>`.'),
  q('git_cmd_9', 'Basic Commands', 'Hard',
    'What does `git log -S "secret_function_name"` (the "pickaxe" option) do?',
    ['Prints code in colorful syntax', 'Searches commit history for commits that introduced or removed instances of the specific string "secret_function_name" in the diff', 'Deletes the function across all commits', 'Renames the function across branches'], 1,
    'The `-S` pickaxe option looks for commits that changed the number of occurrences of the specified string in the file, pinpointing the exact commit where a function or variable was added or deleted.'),
  q('git_cmd_10', 'Basic Commands', 'Hard',
    'What is the difference between `git rm <file>` and `git rm --cached <file>`?',
    ['`git rm` deletes from index only; `--cached` deletes from disk', '`git rm` stages deletion AND deletes the physical file from the local filesystem; `git rm --cached` un-tracks the file from Git index while preserving the physical file safely on disk', '`git rm` cannot be undone', '`--cached` is only for web browsers'], 1,
    '`git rm --cached` is the standard command when a file was accidentally committed (like `.env` or credentials): it stops tracking the file in Git without deleting the file from your local hard drive.'),

  q('git_bm_1', 'Git Branching & Merging', 'Easy',
    'What physically is a "Branch" in Git?',
    ['A complete duplicate copy of the entire codebase in a separate folder', 'A lightweight, movable pointer (a 41-byte text file) containing the 40-character SHA-1 hash of a commit', 'A hardware thread in the CPU', 'An encrypted zip archive'], 1,
    'In Git, branches are simply lightweight pointers referencing specific commit hashes in `.git/refs/heads/`. Creating a branch takes virtually zero disk space and milliseconds.'),
  q('git_bm_2', 'Git Branching & Merging', 'Easy',
    'What command creates and immediately switches to a new branch named "feature-login"?',
    ['git branch -new feature-login', 'git checkout -b feature-login (or git switch -c feature-login)', 'git make branch feature-login', 'git goto feature-login'], 1,
    '`git checkout -b <branch>` (or modern `git switch -c <branch>`) creates the new branch pointer and updates HEAD to point to it immediately.'),
  q('git_bm_3', 'Git Branching & Merging', 'Easy',
    'What is a "Fast-Forward" merge in Git?',
    ['A merge that runs in under 1 second', 'When there are no diverging commits on the target branch, Git simply moves the branch pointer forward to point to the incoming commit without creating a merge commit', 'A merge performed automatically by GitHub', 'Merging without running tests'], 1,
    'A fast-forward merge occurs when the target branch HEAD is a direct ancestor of the incoming branch. Git simply moves the branch pointer forward to the tip of the incoming branch.'),
  q('git_bm_4', 'Git Branching & Merging', 'Easy',
    'When does a "Merge Conflict" occur in Git?',
    ['When two developers have the same name', 'When competing changes are made to the exact same lines of a file in two branches being merged, preventing Git from resolving the diff automatically', 'When a repository exceeds 1 GB', 'When Git server runs out of disk space'], 1,
    'A merge conflict occurs when the 3-way merge algorithm detects conflicting edits to the exact same line ranges in two converging branches, pausing and requiring human manual resolution.'),
  q('git_bm_5', 'Git Branching & Merging', 'Medium',
    'How does Git\'s "3-Way Merge" work when a fast-forward merge is impossible?',
    ['It merges 3 branches simultaneously', 'It uses 3 snapshots: the Common Ancestor commit (base), the tip of Branch A, and the tip of Branch B, computing diffs relative to the common ancestor to create a Merge Commit with two parents', 'It asks 3 developers to vote', 'It executes 3 unit test suites'], 1,
    'A 3-way merge identifies the Best Common Ancestor of the two branches, computes the two sets of diffs against that ancestor, and synthesizes them into a new Merge Commit with two parent pointers.'),
  q('git_bm_6', 'Git Branching & Merging', 'Medium',
    'What do the conflict markers `<<<<<<<`, `=======`, and `>>>>>>>` indicate inside a conflicted file?',
    ['HTML tags for rendering diffs', '`<<<<<<< HEAD` marks changes on current branch; `=======` is the divider; `>>>>>>> branch-name` marks incoming changes from the branch being merged', 'Compiler error codes', 'Encrypted hashes'], 1,
    'Git writes conflict markers into the file: content between `<<<<<<< HEAD` and `=======` is your current branch; content between `=======` and `>>>>>>>` is the incoming branch.'),
  q('git_bm_7', 'Git Branching & Merging', 'Medium',
    'How do you force Git to create a merge commit even if a Fast-Forward merge is possible?',
    ['git merge --force', 'git merge --no-ff <branch>', 'git merge --create-commit', 'git merge --commit-always'], 1,
    '`git merge --no-ff <branch>` forces Git to generate a dedicated merge commit, preserving the historical existence and visual boundary of the feature branch in the log graph.'),
  q('git_bm_8', 'Git Branching & Merging', 'Medium',
    'What command safely deletes a local branch that has already been fully merged into your current branch?',
    ['git branch -D <branch>', 'git branch -d <branch>', 'git delete-branch <branch>', 'git rm -branch <branch>'], 1,
    '`git branch -d <branch>` deletes the branch safely (failing with a warning if the branch contains unmerged commits). `git branch -D` forces deletion unconditionally.'),
  q('git_bm_9', 'Git Branching & Merging', 'Hard',
    'What is "git rerere" (Reuse Recorded Resolution) and how does it assist with long-lived branches?',
    ['A command that repeats the last commit', 'A feature that records how you resolved conflicting merge hunks and automatically applies the identical resolution when the same conflict occurs again in future merges/rebases', 'A rollback tool for failed pushes', 'A tool that deletes remote branches'], 1,
    '`git rerere` records conflict resolutions: if you rebase or re-merge a long-lived feature branch repeatedly, Git remembers how you previously resolved conflicts and applies your fixes automatically.'),
  q('git_bm_10', 'Git Branching & Merging', 'Hard',
    'What is an "Octopus Merge" in Git?',
    ['A merge that fails 8 times', 'A merge strategy that merges more than two branches into a single commit simultaneously, used typically for bundling multiple verified feature branches into a release candidate', 'A GitHub Action with 8 parallel jobs', 'A merge between 8 different repositories'], 1,
    'An octopus merge merges 3 or more branches at once into a single commit with 3+ parents. It refuses to proceed if any merge conflicts exist, requiring clean independent branches.'),

  q('git_rb_1', 'Git Rebase vs Merge', 'Easy',
    'What does `git rebase main` do when executed while on a feature branch?',
    ['Deletes the feature branch and switches to main', 'Replays the feature branch commits on top of the tip of the main branch, creating a clean linear project history', 'Merges main with a merge commit', 'Pushes commits to origin/main'], 1,
    '`git rebase main` finds the common ancestor, detaches feature commits temporarily, resets the feature branch to `main`, and reapplies each feature commit sequentially on top of `main`.'),
  q('git_rb_2', 'Git Rebase vs Merge', 'Easy',
    'What is the "Golden Rule of Rebasing"?',
    ['Never rebase on a Monday', 'Never rebase commits that have been pushed to a shared public/remote branch where others are collaborating', 'Always squash commits before rebasing', 'Never rebase more than 3 commits'], 1,
    'Rebasing rewrites commit SHA hashes. If you rebase public shared branches, you create divergent histories for collaborators, forcing painful force-pulls and merge chaos.'),
  q('git_rb_3', 'Git Rebase vs Merge', 'Easy',
    'Which flag opens the interactive rebasing menu for the last 3 commits?',
    ['git rebase -i HEAD~3', 'git rebase --interactive 3', 'git rebase -menu 3', 'git rebase --edit-last 3'], 0,
    '`git rebase -i HEAD~3` launches an interactive editor allowing you to reorder, squash, edit, reword, or drop the last 3 commits.'),
  q('git_rb_4', 'Git Rebase vs Merge', 'Easy',
    'In an interactive rebase todo list, what does the `squash` (or `s`) command do?',
    ['Deletes the commit entirely', 'Combines the commit into the previous commit in the list and prompts to combine their commit messages', 'Moves the commit to the bottom', 'Signs the commit with GPG'], 1,
    '`squash` melds the selected commit into the commit directly preceding it, combining both sets of changes and allowing you to merge the commit messages together.'),
  q('git_rb_5', 'Git Rebase vs Merge', 'Medium',
    'What is the difference between `squash` and `fixup` in interactive rebase?',
    ['`fixup` is only for bug fixes', 'Both combine changes into the previous commit; `squash` retains both commit messages for editing, while `fixup` discards the current commit\'s message in favor of the previous commit\'s message', '`fixup` creates a new branch', '`squash` cannot be undone'], 1,
    '`fixup` (or `f`) combines the changes into the previous commit but automatically discards the fixup commit message, keeping history clean without opening an editor for messages.'),
  q('git_rb_6', 'Git Rebase vs Merge', 'Medium',
    'What is the primary advantage of Git Rebase over Git Merge in repository history?',
    ['Rebase uses less disk space in `.git`', 'Rebase produces a clean, perfectly linear commit history without cluttering the log graph with hundreds of non-informative "Merge branch" commits', 'Rebase is completely safe on public branches', 'Rebase prevents all merge conflicts'], 1,
    'Rebasing eliminates unnecessary merge bubbles and diamond patterns, making `git log` and `git bisect` linear and straightforward to navigate.'),
  q('git_rb_7', 'Git Rebase vs Merge', 'Medium',
    'What happens when a conflict occurs during a rebase?',
    ['The entire repository is corrupted', 'Rebase pauses at the conflicting commit; you resolve the conflict, stage files with `git add`, and run `git rebase --continue` (without committing)', 'Git automatically reverts all commits', 'HEAD moves to origin/main'], 1,
    'During rebase, Git applies commits one by one. When a conflict arises, rebase pauses. You fix the files, run `git add <file>`, and execute `git rebase --continue` to proceed.'),
  q('git_rb_8', 'Git Rebase vs Merge', 'Medium',
    'How can you completely abort an in-progress rebase and restore your branch to its original state?',
    ['git rebase --stop', 'git rebase --abort', 'git rebase --undo', 'git reset --hard origin/main'], 1,
    '`git rebase --abort` cancels the entire rebase operation and restores the branch pointer and working tree to the exact state before `git rebase` was initiated.'),
  q('git_rb_9', 'Git Rebase vs Merge', 'Hard',
    'What does `git pull --rebase` do compared to a standard `git pull`?',
    ['Deletes local uncommitted files', 'Instead of creating a merge commit when remote has new commits, it fetches remote commits and replays your local unpushed commits on top of origin', 'Rebases remote onto your local machine', 'Pushes changes to remote forcibly'], 1,
    '`git pull --rebase` avoids ugly merge commits created when pulling upstream updates by rebasing your unpublished local work on top of the newly fetched remote tracking branch.'),
  q('git_rb_10', 'Git Rebase vs Merge', 'Hard',
    'What does the `--onto` argument do in `git rebase --onto master featureA featureB`?',
    ['Copies files from featureA to featureB', 'Transplants commits on `featureB` that branched off `featureA` directly onto `master`, severing the dependency on `featureA`', 'Merges all three branches simultaneously', 'Renames featureA to master'], 1,
    '`git rebase --onto <new-base> <old-base> <branch>` extracts only the commits unique to `<branch>` since `<old-base>` and grafts them onto `<new-base>`, ideal for detaching nested feature branches.'),

  q('git_uc_1', 'Undoing Changes', 'Easy',
    'What does `git checkout -- <file>` (or `git restore <file>`) do to unstaged modifications in your working directory?',
    ['Stages the file for commit', 'Discards all unstaged changes in the file, reverting it to match the staging area/HEAD', 'Deletes the file from disk permanently', 'Pushes the file to GitHub'], 1,
    '`git restore <file>` discards local uncommitted modifications in the working copy, reverting the file back to the last staged or committed state.'),
  q('git_uc_2', 'Undoing Changes', 'Easy',
    'How do you unstage a file that was added with `git add` without losing your local changes?',
    ['git restore --staged <file> (or git reset HEAD <file>)', 'git rm <file>', 'git clean -f', 'git revert <file>'], 0,
    '`git restore --staged <file>` (or `git reset HEAD <file>`) moves the file out of the staging area back into unstaged modified state, keeping working directory edits intact.'),
  q('git_uc_3', 'Undoing Changes', 'Easy',
    'What is the difference between `git revert <commit>` and `git reset <commit>`?',
    ['`git reset` is for GitHub; `git revert` is for local', '`git revert` creates a new inverse commit that cancels out changes safely without rewriting history; `git reset` moves the branch pointer backward, rewriting history', '`git revert` deletes files permanently', 'They are exact synonyms'], 1,
    '`git revert` is safe for public branches because it appends a new forward commit undoing previous changes. `git reset` rewrites history by moving the branch pointer back in time.'),
  q('git_uc_4', 'Undoing Changes', 'Easy',
    'What does `git clean -fd` do?',
    ['Formats all Python and JavaScript code', 'Forcefully deletes all untracked files (`-f`) and untracked directories (`-d`) from the working directory', 'Deletes the `.git` directory', 'Cleans up remote branches on GitHub'], 1,
    '`git clean -fd` scrubs the working tree by removing all untracked files and directories that are not in Git index (excluding `.gitignore` unless `-x` is passed).'),
  q('git_uc_5', 'Undoing Changes', 'Medium',
    'What are the three modes of `git reset` and how do they differ regarding Working Directory and Index?',
    ['Low, Medium, High', '`--soft`: moves HEAD only (keeps changes staged); `--mixed` (default): moves HEAD and resets Index (keeps changes unstaged in working tree); `--hard`: moves HEAD and discards all changes in Index and Working Directory', '`--soft` deletes files; `--hard` saves to cloud', 'There is only one reset mode'], 1,
    'Reset modes: `--soft` leaves all changes staged in index; `--mixed` (default) un-stages changes into working directory; `--hard` obliterates all uncommitted edits in both index and working tree.'),
  q('git_uc_6', 'Undoing Changes', 'Medium',
    'If you accidentally executed `git reset --hard HEAD~1` and lost an important commit, how can you recover it?',
    ['It is impossible; the commit is gone forever', 'Use `git reflog` to find the commit\'s SHA-1 hash from HEAD history and run `git reset --hard <SHA>` or `git checkout -b recover <SHA>`', 'Reinstall Git', 'Call GitHub customer support'], 1,
    'Git rarely deletes committed data immediately. `git reflog` logs every movement of HEAD. You find the lost commit hash in the reflog and reset or branch to it safely.'),
  q('git_uc_7', 'Undoing Changes', 'Medium',
    'What does `git revert -m 1 <merge-commit-sha>` do and why is the `-m` flag required?',
    ['Multiplies the commit by 1', 'Reverts a merge commit: the `-m 1` flag specifies parent number 1 (the mainline branch) as the baseline to revert to, because merge commits have two parents', 'Deletes branch 1', 'Reverts 1 file only'], 1,
    'Because a merge commit has multiple parent pointers, Git cannot guess which branch should be considered the mainline. `-m 1` declares parent 1 as the mainline to preserve.'),
  q('git_uc_8', 'Undoing Changes', 'Medium',
    'What does `git restore --source=HEAD~2 <file>` do?',
    ['Restores deleted branches', 'Restores the specified file to the exact state it was in 2 commits ago in your working directory', 'Deletes the file 2 times', 'Reverts the entire repo 2 commits back'], 1,
    '`git restore --source=<commit> <file>` pulls the version of `<file>` from any specified commit or branch and writes it into your current working tree.'),
  q('git_uc_9', 'Undoing Changes', 'Hard',
    'What happens if you revert a merge commit on `main`, continue working on the feature branch, and later attempt to merge the feature branch back into `main`?',
    ['Git automatically re-applies all reverted commits', 'Git will NOT re-apply the original feature commits because they already exist in the history of `main`; you must first revert the revert commit before re-merging', 'A fatal syntax error occurs', 'The repository is corrupted'], 1,
    'Linus Torvalds documented this classic trap: because the original commits exist in main\'s history, a new merge treats them as already present. You must revert the revert commit on `main` to restore the code.'),
  q('git_uc_10', 'Undoing Changes', 'Hard',
    'What is the Git Object Prune threshold and how long do dangling commits stay recoverable in `.git/reflog` by default?',
    ['1 day', '90 days for reachable reflog entries (30 days for unreachable/dangling objects) before `git gc` prunes them permanently', '10 minutes', 'Infinite (never pruned)'], 1,
    'Git retains reflog entries for 90 days (`gc.reflogExpire`) and unreachable dangling commits for 30 days (`gc.reflogExpireUnreachable`), providing a safety window to recover lost commits.'),

  q('git_sp_1', 'Git Stash & Git Cherry-pick', 'Easy',
    'What does `git stash` do when you have uncommitted work in your working tree?',
    ['Deletes your uncommitted work permanently', 'Temporarily shelves (stashes) your modified tracked files and staged changes, giving you a clean working directory', 'Pushes changes to remote GitHub stash', 'Creates a new branch named stash'], 1,
    '`git stash` saves dirty working tree state and index on a storage stack and resets the working directory to match HEAD, letting you switch branches cleanly without committing.'),
  q('git_sp_2', 'Git Stash & Git Cherry-pick', 'Easy',
    'What command restores the most recently stashed changes and removes them from the stash stack?',
    ['git stash apply', 'git stash pop', 'git stash drop', 'git stash clear'], 1,
    '`git stash pop` applies the changes from the top of the stash stack (`stash@{0}`) to your working directory and immediately deletes it from the stash list. `git stash apply` leaves it in the list.'),
  q('git_sp_3', 'Git Stash & Git Cherry-pick', 'Easy',
    'What does `git cherry-pick <commit-hash>` do?',
    ['Selects random fruits from Git', 'Applies the changes introduced by a specific existing commit from another branch onto your current branch as a new commit', 'Picks the best branch automatically', 'Deletes the specified commit'], 1,
    '`git cherry-pick` extracts the diff of a specific commit on another branch and replays it onto your current HEAD as a brand new commit with a new SHA.'),
  q('git_sp_4', 'Git Stash & Git Cherry-pick', 'Easy',
    'By default, does `git stash` save untracked (newly created) files?',
    ['Yes, always', 'No, by default it only stashes tracked modified files; to include untracked files you must pass `-u` (or `--include-untracked`)', 'Yes, but only if they are text files', 'It deletes untracked files'], 1,
    'Standard `git stash` ignores untracked files. To stash newly created files along with modifications, use `git stash -u` (or `git stash -a` for all including ignored files).'),
  q('git_sp_5', 'Git Stash & Git Cherry-pick', 'Medium',
    'How can you view a list of all currently stored stashes and inspect their contents without applying them?',
    ['`git stash list` to view list; `git stash show -p stash@{0}` to view diff patch', '`git log --stash`', '`git view stash`', '`cat .git/stash`'], 0,
    '`git stash list` lists all stashes in order (`stash@{0}`, `stash@{1}`). `git stash show -p <stash>` prints the full unified diff of stashed changes.'),
  q('git_sp_6', 'Git Stash & Git Cherry-pick', 'Medium',
    'How do you give a custom descriptive message to a stash for easier identification later?',
    ['git stash --message "work in progress on navbar"', 'git stash save "work in progress on navbar" (or git stash push -m "description")', 'git stash -title "description"', 'git stash label "description"'], 1,
    '`git stash push -m "WIP on navbar"` annotates the stash entry with a readable description in `git stash list` rather than default generic branch commit messages.'),
  q('git_sp_7', 'Git Stash & Git Cherry-pick', 'Medium',
    'What does `git cherry-pick -n` (or `--no-commit`) do?',
    ['Cancels cherry picking', 'Applies the changes of the selected commit into your working directory and staging area without automatically creating a commit, allowing you to combine or edit changes', 'Prevents tests from running', 'Pushes commit to GitHub'], 1,
    '`git cherry-pick -n` stages the diff into your index without executing a commit, allowing you to inspect, modify, or bundle multiple cherry-picks into a single cohesive commit.'),
  q('git_sp_8', 'Git Stash & Git Cherry-pick', 'Medium',
    'What command creates and checks out a new branch directly from a stash entry?',
    ['git branch -stash <name>', 'git stash branch <new-branch-name> [stash@{n}]', 'git checkout -b from-stash', 'git stash checkout'], 1,
    '`git stash branch <name>` creates a new branch starting at the commit where the stash was originally created, applies the stash changes, and drops the stash if successful.'),
  q('git_sp_9', 'Git Stash & Git Cherry-pick', 'Hard',
    'Under what circumstances can a `git cherry-pick` fail with a conflict?',
    ['Only if commit hashes match', 'When the changes introduced by the cherry-picked commit depend on or modify lines that have evolved differently on the target branch since the commit was authored', 'When the author email is different', 'Cherry pick never conflicts'], 1,
    'Like a merge, cherry-pick applies a diff against the commit\'s parent. If the target branch code has diverged around those lines, Git cannot resolve changes automatically and flags a conflict.'),
  q('git_sp_10', 'Git Stash & Git Cherry-pick', 'Hard',
    'How is a Git Stash physically stored in the `.git` directory under the hood?',
    ['As a temporary text file in `/tmp`', 'As special commit objects with 2 (or 3) parents referenced by `.git/refs/stash`: Parent 1 is current commit, Parent 2 is staged index, Parent 3 is untracked files', 'In an SQLite database', 'In the Windows Registry'], 1,
    'A stash is actually a commit with multiple parents: one parent is HEAD, one parent represents the index state at stash time, and an optional third parent holds untracked files.'),

  q('git_rc_1', 'Remote Repositories & Collaboration', 'Easy',
    'What is the default conventional name assigned to the primary remote repository when cloning?',
    ['master', 'main', 'origin', 'upstream'], 2,
    'When you clone a repository, Git automatically assigns the remote alias `origin` pointing to the cloned URL.'),
  q('git_rc_2', 'Remote Repositories & Collaboration', 'Easy',
    'What is the fundamental difference between `git fetch` and `git pull`?',
    ['`git pull` is for SSH; `git fetch` is for HTTPS', '`git fetch` downloads commits, refs, and tags from remote without modifying local branches; `git pull` runs `git fetch` followed immediately by `git merge` into current branch', '`git fetch` deletes local branches', '`git pull` only downloads commit messages'], 1,
    '`git fetch` updates remote tracking branches (`origin/main`) safely without touching your working files. `git pull` executes `git fetch` and immediately merges (`git merge origin/main`) into your active branch.'),
  q('git_rc_3', 'Remote Repositories & Collaboration', 'Easy',
    'What does the `-u` (or `--set-upstream`) flag do in `git push -u origin feature`?',
    ['Unlocks the branch on GitHub', 'Sets `origin/feature` as the default upstream tracking reference for the local `feature` branch, allowing subsequent `git pull` and `git push` without arguments', 'Deletes the branch after push', 'Enables user authentication'], 1,
    '`-u` links the local branch to the specified remote branch, establishing tracking configuration in `.git/config` so you can simply type `git push` or `git pull` later.'),
  q('git_rc_4', 'Remote Repositories & Collaboration', 'Easy',
    'What command lists the remote repository URLs configured for your local repo?',
    ['git remote -v', 'git url -list', 'git remote show-all', 'git config --remotes'], 0,
    '`git remote -v` displays the names and fetch/push URLs of all configured remotes.'),
  q('git_rc_5', 'Remote Repositories & Collaboration', 'Medium',
    'In open-source fork workflows, what is the convention regarding `origin` vs `upstream` remotes?',
    ['`origin` points to original maintainer repo; `upstream` is your fork', '`origin` points to your personal GitHub fork (read/write); `upstream` points to the original upstream open-source repository (read-only) to sync latest updates', '`upstream` is for SVN only', 'They must point to identical URLs'], 1,
    'In forking workflows, `origin` is your personal fork where you push branches. `upstream` points to the main organizational repository from which you fetch upstream releases.'),
  q('git_rc_6', 'Remote Repositories & Collaboration', 'Medium',
    'What is the danger of running `git push --force` and what safer alternative exists?',
    ['It shuts down GitHub servers', 'It overwrites remote history unconditionally, destroying commits pushed by colleagues; the safer flag `git push --force-with-lease` refuses to overwrite if remote has commits you haven\'t fetched', '`git push --force` deletes your local code', 'There is no alternative'], 1,
    '`git push --force-with-lease` checks if the remote ref matches your local remote tracking ref before overwriting. If a colleague pushed new commits in the interim, the push is rejected safely.'),
  q('git_rc_7', 'Remote Repositories & Collaboration', 'Medium',
    'What command deletes a remote branch named `feature-old` on `origin`?',
    ['git branch -d origin/feature-old', 'git push origin --delete feature-old (or git push origin :feature-old)', 'git remote rm feature-old', 'git delete-remote feature-old'], 1,
    '`git push origin --delete <branch>` (or pushing null ref `git push origin :<branch>`) tells the remote server to delete that branch reference.'),
  q('git_rc_8', 'Remote Repositories & Collaboration', 'Medium',
    'What does `git remote prune origin` (or `git fetch --prune`) do?',
    ['Deletes all branches on GitHub', 'Deletes local stale remote-tracking branches (e.g. `origin/old-feature`) that no longer exist on the remote repository', 'Compresses remote images', 'Removes Git credentials'], 1,
    'When branches are deleted or merged on GitHub, your local repo retains dead `origin/xyz` tracking pointers. Running `git fetch --prune` purges these stale references locally.'),
  q('git_rc_9', 'Remote Repositories & Collaboration', 'Hard',
    'How does Git authenticate with remote servers via SSH keys vs Personal Access Tokens (PAT)?',
    ['SSH keys require passwords on every commit; PAT does not', 'SSH uses asymmetric public-private keypairs (`~/.ssh/id_ed25519`); PATs are granular alphanumeric tokens passed over HTTPS replacing account passwords for API and Git operations', 'PATs only work on Windows', 'SSH keys work only for read operations'], 1,
    'GitHub deprecated password authentication over HTTPS in favor of scoped Personal Access Tokens (PAT). SSH uses public key cryptography (`id_ed25519`) configured in GitHub user settings.'),
  q('git_rc_10', 'Remote Repositories & Collaboration', 'Hard',
    'What is Git LFS (Large File Storage) and how does it prevent repository bloat?',
    ['A hard drive sold by GitHub', 'An extension that replaces large files (videos, models, datasets) in the Git repository with small pointer text files, storing the actual heavy binary payloads on external storage servers', 'A tool that zips Git repositories', 'A cloud backup service for RAM'], 1,
    'Because Git tracks all historical file revisions, committing 500 MB binaries bloats repository clone sizes forever. Git LFS stores small pointer text files in Git while moving heavy binary data to cloud blob storage.'),

  q('git_bs_1', 'Git Bisect & Debugging Tools', 'Easy',
    'What is `git blame <file>` used for?',
    ['To insult the author of bad code', 'To examine file contents line-by-line, showing the commit hash, author, and date of the last modification for every single line in the file', 'To run automated tests', 'To list all syntax errors in a file'], 1,
    '`git blame` annotates each line of a file with the author name, commit hash, and timestamp of the commit that last touched that line, pinpointing who wrote it and why.'),
  q('git_bs_2', 'Git Bisect & Debugging Tools', 'Easy',
    'What algorithmic technique does `git bisect` use to pinpoint the commit that introduced a bug?',
    ['Linear Search', 'Binary Search (O(log N))', 'Breadth-First Search', 'Depth-First Search'], 1,
    '`git bisect` uses binary search: by repeatedly halving the commit range between a known "good" and "bad" commit, it can pinpoint a bug introduced among 1,000 commits in approximately 10 steps.'),
  q('git_bs_3', 'Git Bisect & Debugging Tools', 'Easy',
    'What are the first two commands you run to start a `git bisect` session?',
    ['`git bisect run` and `git bisect end`', '`git bisect start` followed by marking known states: `git bisect bad` (current broken commit) and `git bisect good <commit-sha>` (known working commit)', '`git bisect find-bug`', '`git bisect fix`'], 1,
    'You initialize with `git bisect start`, tag the current buggy HEAD as `git bisect bad`, and tag an older known functional commit as `git bisect good v1.0`.'),
  q('git_bs_4', 'Git Bisect & Debugging Tools', 'Easy',
    'What command terminates a `git bisect` session and returns your branch to the original commit before bisecting began?',
    ['git bisect stop', 'git bisect reset', 'git bisect exit', 'git bisect cancel'], 1,
    '`git bisect reset` cleans up bisect references and restores HEAD back to the original branch/commit where you were before starting the bisect session.'),
  q('git_bs_5', 'Git Bisect & Debugging Tools', 'Medium',
    'How can you fully automate `git bisect` to locate a bug without manual human testing on every step?',
    ['Git bisect cannot be automated', 'By providing an automated test script: `git bisect run ./test_script.sh` (where script exits 0 for good and 1-127 for bad)', 'By connecting Git to an AI chat model', 'By running `git bisect --auto`'], 1,
    '`git bisect run <script>` automatically checks out each bisect midpoint, executes the script (e.g. `npm test`), and marks good or bad based on the script\'s exit code (0 = success/good, non-zero = fail/bad) until the culprit commit is found.'),
  q('git_bs_6', 'Git Bisect & Debugging Tools', 'Medium',
    'What does `git bisect skip` do when encountering a commit that cannot be tested (e.g. build fails due to unrelated error)?',
    ['Aborts the entire bisect session', 'Instructs Git to skip the current commit and choose a nearby commit to test instead', 'Marks the commit as good', 'Deletes the broken commit'], 1,
    'If a commit has broken dependencies or won\'t compile for unrelated reasons, `git bisect skip` tells Git to test an adjacent commit instead.'),
  q('git_bs_7', 'Git Bisect & Debugging Tools', 'Medium',
    'What does `git log -p -2 <file>` do?',
    ['Prints the last 2 lines of the file', 'Shows the commit history of the specified file for the last 2 commits along with the full unified patch diff of changes', 'Deletes 2 commits from history', 'Compiles the file twice'], 1,
    '`-p` generates unified diff patches for each commit, and `-2` limits the log output to the last 2 commits touching that specific file.'),
  q('git_bs_8', 'Git Bisect & Debugging Tools', 'Medium',
    'What does `git log --grep="CVE-2024"` do?',
    ['Searches file contents for the CVE text', 'Filters and displays only commits whose commit log message contains the regex/string "CVE-2024"', 'Scans for network vulnerabilities', 'Creates a security branch'], 1,
    '`--grep` searches commit messages for matches, making it easy to find commits referencing specific bug ticket IDs or security advisories.'),
  q('git_bs_9', 'Git Bisect & Debugging Tools', 'Hard',
    'What does `git log -L 15,25:src/index.js` (line-level history) do?',
    ['Prints lines 15 through 25 to terminal', 'Traces the complete historical evolution and diffs of specifically lines 15 to 25 within `src/index.js` across all past commits', 'Locks lines 15 to 25 from being edited', 'Deletes lines 15 to 25'], 1,
    'The `-L` flag traces line-range evolution across repository history, showing every commit that altered code within that specific line range over time.'),
  q('git_bs_10', 'Git Bisect & Debugging Tools', 'Hard',
    'What is `git fsck` (File System Consistency Check)?',
    ['A tool that formats the hard disk', 'A diagnostic command that verifies the connectivity, cryptographic integrity, and consistency of objects in the Git database, identifying corrupted blobs and dangling commits', 'A tool that checks network latency', 'A unit test framework for Git'], 1,
    '`git fsck` validates the internal object database, checking SHA hashes for corruption, finding unreachable orphan objects, and ensuring the Merkle tree DAG is intact.'),

  q('git_hk_1', 'Git Hooks & Workflow Automation', 'Easy',
    'Where are local Git hooks stored by default in a repository?',
    ['.git/hooks/', '.github/workflows/', 'src/hooks/', 'node_modules/git/'], 0,
    'Git hooks are executable client-side and server-side scripts located in the `.git/hooks/` directory, triggered by actions like committing, merging, or pushing.'),
  q('git_hk_2', 'Git Hooks & Workflow Automation', 'Easy',
    'Which Git hook executes before a commit message is even prompted, allowing automated linting and formatting?',
    ['post-commit', 'pre-commit', 'commit-msg', 'pre-push'], 1,
    'The `pre-commit` hook runs before a commit is created. If the script exits with non-zero status (e.g. linter fails), Git aborts the commit immediately.'),
  q('git_hk_3', 'Git Hooks & Workflow Automation', 'Easy',
    'What popular Node.js tool makes managing and sharing Git hooks easy across teams via `package.json`?',
    ['Webpack', 'Husky', 'Babel', 'Prettier'], 1,
    'Husky is a widely adopted tool that allows Git hooks to be version-controlled and installed automatically on `npm install`, ensuring all team members run the same pre-commit checks.'),
  q('git_hk_4', 'Git Hooks & Workflow Automation', 'Easy',
    'What hook is commonly used to validate that commit messages follow Conventional Commits format (e.g. `feat: add auth`)?',
    ['pre-commit', 'commit-msg', 'post-merge', 'pre-rebase'], 1,
    'The `commit-msg` hook takes the path to the temporary file containing the commit message as an argument, allowing tools (like `commitlint`) to reject non-conforming messages.'),
  q('git_hk_5', 'Git Hooks & Workflow Automation', 'Medium',
    'Why are scripts in `.git/hooks/` NOT automatically tracked or pushed to remote repositories by standard Git?',
    ['Because Git hooks only work in Python', 'For security reasons: allowing remotes to automatically install and execute arbitrary scripts on developers\' machines upon clone would create massive remote code execution vulnerabilities', 'Because Git hooks are too large in size', 'Because `.git` cannot store files'], 1,
    'If Git hooks were pushed and auto-executed on clone, cloning a malicious repo could run dangerous shell scripts immediately. Tools like Husky or `core.hooksPath` configure shared hooks explicitly.'),
  q('git_hk_6', 'Git Hooks & Workflow Automation', 'Medium',
    'What does `git commit --no-verify` (or `-n`) do?',
    ['Runs all tests twice', 'Bypasses the `pre-commit` and `commit-msg` Git verification hooks, forcing the commit through without running hook validations', 'Verifies commit signature with GPG', 'Deletes uncommitted files'], 1,
    '`git commit --no-verify` skips pre-commit and commit-msg hooks, useful in urgent workarounds but dangerous if broken code is committed without lint checks.'),
  q('git_hk_7', 'Git Hooks & Workflow Automation', 'Medium',
    'What tool is commonly paired with Husky to run linters ONLY on staged files rather than the entire project?',
    ['lint-staged', 'eslint-all', 'git-scanner', 'ts-node'], 0,
    '`lint-staged` inspects git index and executes linters/formatters (Prettier, ESLint) exclusively on files currently staged for commit, keeping pre-commit checks blazing fast.'),
  q('git_hk_8', 'Git Hooks & Workflow Automation', 'Medium',
    'What is the difference between client-side hooks and server-side hooks in Git?',
    ['Client hooks run in browsers; server hooks in cloud', 'Client hooks (pre-commit, pre-push) run locally on developer machines and can be bypassed; Server hooks (pre-receive, update) run on the central Git server and enforce non-bypassable enterprise policies', 'Server hooks only format code', 'There is no difference'], 1,
    'Client-side hooks assist developer productivity locally. Server-side hooks (like `pre-receive` on GitHub Enterprise / GitLab) run on push to enforce company-wide security, branch naming, and signing policies unconditionally.'),
  q('git_hk_9', 'Git Hooks & Workflow Automation', 'Hard',
    'What Git configuration option (introduced in Git 2.9) allows pointing Git hooks to a version-controlled directory in the project repository?',
    ['git.hooksDirectory', 'core.hooksPath', 'hooks.location', 'git.sharedHooks'], 1,
    '`git config core.hooksPath .githooks` reconfigures Git to load hook scripts from `.githooks/` inside the repo root, allowing hooks to be tracked in version control cleanly without Husky.'),
  q('git_hk_10', 'Git Hooks & Workflow Automation', 'Hard',
    'In a server-side `pre-receive` hook, what standard input does Git pass to the script?',
    ['JSON object with pull request info', 'Lines containing: `<old-value> <new-value> <ref-name>` for each ref being updated, allowing rejection of non-fast-forward pushes or unapproved branches', 'The full commit patch file', 'A list of developer SSH keys'], 1,
    '`pre-receive` reads lines from stdin formatted as `<old-sha> <new-sha> <refname>`. If the script exits non-zero, all ref updates in the push are rejected in a single atomic transaction.'),

  q('git_bs_strat_1', 'Branching Strategies', 'Easy',
    'What are the two permanent, infinite-lifetime branches in the classic "Git Flow" branching model?',
    ['alpha and beta', 'main (or master) and develop', 'feature and bugfix', 'staging and production'], 1,
    'In Vincent Driessen\'s Git Flow, `main` (stores production-ready releases) and `develop` (stores delivered development integration) are the two primary long-lived branches.'),
  q('git_bs_strat_2', 'Branching Strategies', 'Easy',
    'What is the core philosophy of "Trunk-Based Development" (TBD)?',
    ['Developers work on branches that last 6 months', 'All developers merge small, frequent updates into a single central branch ("trunk" / main) at least once daily, avoiding long-lived feature branches', 'Never use branches at all', 'Developing software inside Docker trunks'], 1,
    'Trunk-Based Development emphasizes continuous integration: developers work in short-lived feature branches (lasting hours or 1-2 days) or commit directly to trunk, relying on feature flags and automated CI.'),
  q('git_bs_strat_3', 'Branching Strategies', 'Easy',
    'What is the core workflow of "GitHub Flow"?',
    ['A 12-branch complex waterfall model', 'A simple lightweight workflow: anything on `main` is deployable; create feature branches off `main`, open a Pull Request for discussion/review, and merge into `main` after CI passes', 'A workflow with no pull requests', 'Deploying code once every 6 months'], 1,
    'GitHub Flow is lightweight: 1) `main` is always deployable; 2) Create descriptive branch off `main`; 3) Open Pull Request; 4) Review and test; 5) Merge to `main` and deploy immediately.'),
  q('git_bs_strat_4', 'Branching Strategies', 'Easy',
    'In Git Flow, what is a "Hotfix" branch used for?',
    ['Writing new seasonal features', 'Urgent production defect fixes branched directly off `main` and merged back into both `main` and `develop` immediately', 'Refactoring unit tests', 'Testing GPU cooling systems'], 1,
    'Hotfix branches handle critical production emergencies: they branch from `main`, apply the patch, and merge into both `main` (with release tag) and `develop` to prevent regression.'),
  q('git_bs_strat_5', 'Branching Strategies', 'Medium',
    'Why can long-lived feature branches (branches open for weeks/months) create "Merge Hell"?',
    ['Because Git can only store 10 branches', 'The branch diverges significantly from the evolving codebase; resolving massive accumulated conflicts and regressions becomes overwhelmingly difficult and risky', 'Because GitHub charges hourly fees for open branches', 'Because commits expire after 30 days'], 1,
    'As team members continuously modify architecture, long-lived branches fall far behind. Merging them back produces hundreds of complex conflicts and breaks untested assumptions.'),
  q('git_bs_strat_6', 'Branching Strategies', 'Medium',
    'How do "Feature Flags" (Feature Toggles) enable Trunk-Based Development for incomplete features?',
    ['By hiding code files with `.hidden` extension', 'By wrapping incomplete code behind conditional flags (`if (featureFlag.isEnabled)`) so code can be safely merged to trunk and deployed to production continuously while remaining inactive for users', 'By delaying deployments for 2 weeks', 'By encrypting feature commits'], 1,
    'Feature flags decouple code deployment from feature release: developers merge small commits into trunk continuously without exposing unfinished user experiences.'),
  q('git_bs_strat_7', 'Branching Strategies', 'Medium',
    'In GitLab Flow, what problem do "Environment Branches" (e.g. `staging`, `production`) solve?',
    ['They replace Kubernetes clusters', 'They allow downstream promotion where code is merged from feature to `main`, tested in staging via `staging` branch, and promoted to `production` branch matching deployment environments', 'They run on different operating systems', 'They eliminate unit tests'], 1,
    'GitLab Flow introduces environment branches (staging, pre-production, production) where commits flow downstream, ensuring that what was tested in staging is precisely what is released to production.'),
  q('git_bs_strat_8', 'Branching Strategies', 'Medium',
    'In Git Flow, what is the role of a "Release Branch" (e.g. `release-1.2.0`)?',
    ['To develop major brand new features', 'Branched off `develop` when features are complete to allow bug fixes, documentation, and polishing for release, while freeing `develop` to accept new features for future sprints', 'To backup the database', 'To delete older commits'], 1,
    'A release branch provides a feature freeze window: only bug fixes, docs, and metadata tweaks occur here. Once ready, it is merged into `main` and back into `develop`.'),
  q('git_bs_strat_9', 'Branching Strategies', 'Hard',
    'What DORA research metric strongly correlates with Trunk-Based Development compared to heavy branching models like Git Flow?',
    ['Lower salaries for developers', 'Significantly higher Deployment Frequency, shorter Lead Time for Changes, and lower Change Failure Rates (hallmarks of elite DevOps performers)', 'More merge conflicts per day', 'Larger pull request sizes'], 1,
    'State of DevOps (DORA) research demonstrates that elite engineering performance is strongly correlated with Trunk-Based Development, whereas long-lived branch models impede continuous delivery.'),
  q('git_bs_strat_10', 'Branching Strategies', 'Hard',
    'What is "Stacked PRs" (Stacked Diffs) workflow used at companies like Meta and Google?',
    ['Stacking multiple PRs in a physical drawer', 'Breaking a massive feature into a linear chain of small, dependent pull requests where PR 2 branches off PR 1, allowing fast incremental reviews without blocking development', 'Opening 10 identical pull requests', 'Merging pull requests backwards'], 1,
    'Stacked PRs break large initiatives into atomic, easily reviewable pull requests. As each diff is reviewed and approved, the stack is rebased and merged iteratively without review fatigue.')
];

const gitContent = `// js/data/interviewPrep/git.js
// Complete Question Bank for Git & Version Control (10 topics * 10 = 100 MCQs)

window.interviewPrepGit = {
  id: 'git_version_control',
  title: 'Git & Version Control',
  icon: 'source_environment',
  description: 'Master Git internal architecture, basic commands, branching, merging vs rebasing, undoing changes, stashing, remotes, bisect, hooks, and branching strategies.',
  totalQuestions: ${gitQuestions.length},
  topics: [
    'Git Architecture & Object Model',
    'Basic Commands',
    'Git Branching & Merging',
    'Git Rebase vs Merge',
    'Undoing Changes',
    'Git Stash & Git Cherry-pick',
    'Remote Repositories & Collaboration',
    'Git Bisect & Debugging Tools',
    'Git Hooks & Workflow Automation',
    'Branching Strategies'
  ],
  questions: ${JSON.stringify(gitQuestions, null, 2)}
};
`;
fs.writeFileSync(path.join(outDir, 'git.js'), gitContent);
console.log(`Successfully generated git.js (${gitQuestions.length} questions across 10 topics)`);
